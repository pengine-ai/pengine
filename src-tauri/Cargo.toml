[package]
name = "pengine"
version = "1.0.2"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "pengine_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
axum = "0.8"
tower-http = { version = "0.6", features = ["cors"] }
teloxide = { version = "0.17", features = ["macros"] }
reqwest = { version = "0.13", features = ["json"] }
env_logger = "0.11"
chrono = { version = "0.4", features = ["serde"] }
tokio-stream = { version = "0.1", features = ["sync"] }
socket2 = "0.5"
fastrand = "2"
tauri-plugin-dialog = "2"
tauri-plugin-cli = "2"
rustyline = "14"
zip = { version = "2", default-features = false, features = ["deflate"] }
futures = "0.3.31"
regex = "1"
tempfile = "3"

[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3"
core-foundation = "0.10"
# Used to set NSApp's activation policy to Accessory before Tauri starts —
# eliminates the Dock-icon flash on CLI invocations (the bundle's Info.plist
# `LSUIElement=true` covers the .app case; this covers dev/`cargo run`).
objc2 = "0.6"
objc2-app-kit = { version = "0.3", features = ["NSApplication", "NSResponder"] }
objc2-foundation = { version = "0.3", features = ["NSThread"] }

[target.'cfg(target_os = "linux")'.dependencies]
keyring = { version = "3", default-features = false, features = ["sync-secret-service", "crypto-rust"] }

[target.'cfg(target_os = "windows")'.dependencies]
keyring = { version = "3", default-features = false, features = ["windows-native"] }

[dev-dependencies]
ctor = "0.2"

